
import 'package:flutter/material.dart';

class HomeDiaryScreen extends StatelessWidget {
  const HomeDiaryScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('달력을넣을거여요'),
    );
  }
}

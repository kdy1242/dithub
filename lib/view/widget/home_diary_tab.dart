
import 'package:flutter/material.dart';

class HomeDiaryTabItem extends StatelessWidget {
  const HomeDiaryTabItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

